"use client";

// Phase 2C renders the domain workspace navigation once from AppShell.
// Keep this compatibility component so Phase 2B pages do not require rewrites.
export default function WorkspaceTabs({ domain: _domain }: { domain: string }) {
  return null;
}
